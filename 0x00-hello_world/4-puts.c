#include <stdio.h>
/**
 * main - prints the phrase "programming is like building a multilingual puzzel
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void)
{
puts("\"programming is like building a multilinguaul puzzel");
return (0);
}
